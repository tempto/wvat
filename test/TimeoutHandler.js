const TimeoutHandler = require("../src/TimeoutHandler.js");
const {expect, test} = require('@oclif/test')



describe('TimeoutHandler tests', () => {

    /*
    test
    .stdout()
    .command(['hello','-t','1'])
    .catch(err => expect(err.message).equal('EEXIT: 1'))
    .exit(1)
    .it('Test if program timeout')
    */

    

});
